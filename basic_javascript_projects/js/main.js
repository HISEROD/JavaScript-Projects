var awesome = (1 + 1 / 144) ** 144, expression = "(1 + 1 / 144) ** 144 = "; // approximate Euler's number using one of Jacob Bernoulli's formulae
window.alert(expression + awesome);                                         // alert the user of the output of the preceding expression
document.write("Euler's number has been successfully approximated.")        // inform the user of success

function My_First_Function() {              // Defining a function and naming it
    var str = "This is the button text!";   // Defining a variable and giving it
                                            // a string value
    str = str.fontcolor("lightgray");       // make the text light gray
    document.getElementById("Button_Text").innerHTML = str; // Putting the value
                                            // of the variable into the HTML elementFromPoint
                                            // with the "Button_Text" id
}